package main.prefix_sum;

import java.util.concurrent.Callable;

public class DownSummator implements Callable<int[]> {
    @Override
    public int[] call() throws Exception {
        return new int[0];
    }
}
